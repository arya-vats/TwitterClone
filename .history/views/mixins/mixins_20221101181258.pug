mixin createPostForm(userLoggedIn)
    .PostFormContainer
        .userImageContainer
            img(src="userLoggedIn.profilePic.jpeg", alt="User profile picture")
        .textareaAContainer
            textarea#postTextarea(placeholder="What's happening?")   
            .buttonsContainer 
                button#submitPostButton(disabled="") Post 